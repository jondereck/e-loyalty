export default function Loading() {
  return (
    <main className="lp-route-loader">
      <span className="lp-loader-mark" aria-hidden="true" />
      <span>Loading account</span>
    </main>
  );
}
